Removing a secret from a file and committing the change does not remove it from Git history. Git stores every commit permanently, so older commits may still contain the exposed credential. Attackers and secret-scanning tools can inspect previous commits and recover leaked credentials. To fully remove a secret, repository history must be rewritten and the compromised credentials should be rotated immediately.